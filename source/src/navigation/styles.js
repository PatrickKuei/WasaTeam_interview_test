import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  toggleTabButtonContainer: { position: "absolute", bottom: 0, left: "47vw" },
  hideTabButton: { transform: [{ rotate: "180deg" }] },
  showTabButton: {},
});
